{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile",
            "type": "shell",
            "command": "g++",
            "args": [
                "-o",
                "${fileBasenameNoExtension}",
                "${file}"
            ],
            "group": {
                "kind": "build",
                "isDefault": false
            },
            
        },
        {
            "label": "compile and run",
            "type": "shell",
            "command": "g++ -o ${fileBasenameNoExtension} ${file}",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}